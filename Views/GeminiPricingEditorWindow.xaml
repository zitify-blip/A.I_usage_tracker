<Window x:Class="AIUsageTracker.Views.GeminiPricingEditorWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Gemini 단가 편집"
        Width="720" Height="500"
        Background="{StaticResource BgBaseBrush}"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Gemini 단가 편집 (USD / 1M tokens)"
                   FontSize="18" FontWeight="Bold"
                   Foreground="{StaticResource TxtHighlightBrush}" Margin="0,0,0,12"/>

        <Border Grid.Row="1" Style="{StaticResource Card}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="80"/>
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="모델"   Style="{StaticResource Sec}"/>
                    <TextBlock Grid.Column="1" Text="입력($/M)"  Style="{StaticResource Sec}" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="2" Text="출력($/M)"  Style="{StaticResource Sec}" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="3" Text="캐시($/M)"  Style="{StaticResource Sec}" HorizontalAlignment="Right"/>
                    <TextBlock Grid.Column="4" Text="상태"       Style="{StaticResource Sec}" HorizontalAlignment="Center"/>
                </Grid>

                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="RowsHost">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,3">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="80"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding DisplayName}"
                                               Foreground="{StaticResource TxtBrush}" FontSize="11"
                                               VerticalAlignment="Center"/>
                                    <TextBox Grid.Column="1" Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                                             TextAlignment="Right" Margin="3,0"/>
                                    <TextBox Grid.Column="2" Text="{Binding OutputText, UpdateSourceTrigger=PropertyChanged}"
                                             TextAlignment="Right" Margin="3,0"/>
                                    <TextBox Grid.Column="3" Text="{Binding CacheText, UpdateSourceTrigger=PropertyChanged}"
                                             TextAlignment="Right" Margin="3,0"/>
                                    <TextBlock Grid.Column="4" Text="{Binding StatusText}"
                                               Foreground="{Binding StatusBrush}" FontSize="10"
                                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Grid>
        </Border>

        <TextBlock Grid.Row="2"
                   Text="💡 빈 칸이면 프리셋 단가 사용. 값을 비워두고 저장하면 오버라이드가 제거됩니다."
                   FontSize="10" Foreground="{StaticResource TxtHintBrush}" Margin="0,10,0,0"/>

        <Grid Grid.Row="3" Margin="0,12,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="8"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="↺ 모두 프리셋으로 복원"
                    Style="{StaticResource Btn}" Click="ResetAll_Click"/>
            <TextBlock Grid.Column="1" x:Name="StatusLabel" Foreground="{StaticResource TxtSubBrush}" FontSize="11"
                       VerticalAlignment="Center" Margin="12,0,0,0"/>
            <Button Grid.Column="2" Content="닫기" Style="{StaticResource Btn}" Click="Close_Click"/>
            <Button Grid.Column="4" Content="💾 저장" Style="{StaticResource BtnPrimary}"
                    Click="Save_Click"/>
        </Grid>
    </Grid>
</Window>
